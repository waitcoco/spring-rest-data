jena:
  modelName: biluProdFusion
  fusekiURI: http://localhost:864/knowledge

spring:
  datasource:
    url: jdbc:mysql://miami-mysql:q:3306/rizhao
    username: root
    password: miami_rizhao
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone:  http://localhost:862/eureka/