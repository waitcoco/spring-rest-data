jena:
  modelName: biluProdFusion
  fusekiURI: http://DEPLOY_TARGET_MACHINE:864/knowledge

spring:
  datasource:
    url: jdbc:mysql://miami-mysql:3306/miami
    username: miami
    password: miami
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone:  http://DEPLOY_TARGET_MACHINE:862/eureka/